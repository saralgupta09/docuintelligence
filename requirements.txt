# ============================================================
# DocuIntel — Requirements (Phase 1 + Phase 2 + Phase 3 + Phase 4)
# ============================================================
# Install:
# pip install -r requirements.txt
#
# Recommended Python:
# Python 3.11
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── Config ───────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ── PDF Processing ───────────────────────────────────────────
PyMuPDF==1.24.5

# ── LangChain Ecosystem ──────────────────────────────────────
langchain==0.3.20
langchain-core==0.3.45
langchain-community==0.3.19
langchain-text-splitters==0.3.6
langchain-huggingface==0.1.2
langgraph==0.2.74

# ── Embeddings ───────────────────────────────────────────────
sentence-transformers==3.0.1
torch==2.3.1

# GPU users:
# pip install torch==2.3.1+cu118 --index-url https://download.pytorch.org/whl/cu118

# ── Vector Database ──────────────────────────────────────────
chromadb==0.5.3

# ── Gemini ───────────────────────────────────────────────────
google-genai==1.16.1

# ── Phase 3: Hybrid Retrieval ───────────────────────────────
rank-bm25==0.2.2
numpy>=1.26.0

# ── Phase 4: OCR Support ────────────────────────────────────
pytesseract==0.3.13   # Python wrapper for Tesseract OCR
Pillow>=10.0.0        # Image processing (likely already installed)
pdf2image==1.17.0     # PDF→image conversion via poppler (optional fallback)

# System dependency (not pip — install once on your OS):
# Ubuntu/Debian:  sudo apt-get install tesseract-ocr
# macOS:          brew install tesseract
# Verify install: tesseract --version